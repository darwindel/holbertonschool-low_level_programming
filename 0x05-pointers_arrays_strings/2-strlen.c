#include "holberton.h"
#include <stdio.h>
/**
* _strlen - will return the string length
*
* @s: this will go into the prototype
*
* Return: 0 if successful
*
*/
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
