#include "holberton.h"
#include <stdio.h>
/**
* _strlen - will return the string length
*
* @s: this will go into the prototype
*
* Return: 0 if successful
*
*/
int _strlen(char *s)
{
	for (s = 0; s != (void *)0; ++s)

	printf("Length of Str is %s", s);

	return (0);
}
