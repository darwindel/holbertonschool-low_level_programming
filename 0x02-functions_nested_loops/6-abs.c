#include "holberton.h"
/**
* _abs - this will compute the absolute value of an integer
*
* @n: is the int that I will use to put into _abs
*
* Return: 0 if successful
*/
int _abs(int n)
{

	if (n < 0)
	return (-n);
	else
	return (n);
}
