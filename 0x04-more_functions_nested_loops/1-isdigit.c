#include "holberton.h"
/**
* _isdigit - will check for a digit
*
* @c: number that will be tested by _isdigit
*
* Return: 0 if successful
*/
int _isdigit(int c)
{

	if (c >= 0 && c >= 10)
{
	return (1);
}
	else
	return (0);
}
